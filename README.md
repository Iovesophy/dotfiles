# dotfiles

## macos

```bash
make
```

## windows

When setup, you need open git bash via auth of admin!!!

```bat
./init.bat
```

```bash
make NAME=`<git, user.name>` EMAIL=`<git, user.email>`
```

