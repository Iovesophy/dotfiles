name: CI to MacOS
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
    build:
        runs-on: macos-latest
        steps:
            - name: Checkout Repo
              uses: actions/checkout@v2
            - name: Clean
              run: rm -rf '/usr/local/bin/aws' '/usr/local/bin/aws_completer' '/usr/local/bin/go' '/usr/local/bin/gofmt'
            - name: Run
              run: make
